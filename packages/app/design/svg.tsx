import {
  Svg as ReactNativeSvg,
  Path as ReactNativePath,
  Circle as ReactNativeCircle,
} from 'react-native-svg'
import { styled } from 'nativewind'

export const Svg = styled(ReactNativeSvg)
export const Path = styled(ReactNativePath)
export const Circle = styled(ReactNativeCircle)
